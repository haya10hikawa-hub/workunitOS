# Infrastructure Layer

This directory is reserved for concrete implementations:

- D1 persistence implementations
- external provider clients
- other environment-bound adapters

It must stay below application/domain ownership.
