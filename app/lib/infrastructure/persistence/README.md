# Infrastructure Persistence

Target home for concrete persistence implementations and adapters.

Current D1 repositories still live in `app/lib/persistence/d1/` and are treated as infrastructure until a later move.
