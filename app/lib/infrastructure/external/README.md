# Infrastructure External

Target home for concrete external provider clients.

Canonical provider read adapters now live here.

- `github/`
- `slack/`
- `calendar/`

Legacy paths under `app/lib/workunitInbox/sources/` remain compatibility exports only.
