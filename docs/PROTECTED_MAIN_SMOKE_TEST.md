# Protected Main Smoke Test

This temporary file verifies that the `Main Safety Gate` ruleset requires the `validate` CI check before merging into `main`.

This file should be removed after the smoke test is complete.
